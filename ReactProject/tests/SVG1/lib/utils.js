export const camelCase = (a) => a.replace(/-([a-z])/g, (a) => a[1].toUpperCase()); export const camelCaseNodeName = ({ nodeName: a, nodeValue: b }) => ({ nodeName: camelCase(a), nodeValue: b }); export const removePixelsFromNodeValue = ({ nodeName: a, nodeValue: b }) => ({ nodeName: a, nodeValue: b.replace('px', '') }); export const transformStyle = (a, b, c) => 'style' === a ? b.split(';').reduce((a, b) => { const [d, e] = b.split(':'); return '' == d ? a : { ...a, [camelCase(d)]: c && 'fill' === d ? c : e }; }, {}) : null; export const getEnabledAttributes = (a) => ({ nodeName: b }) => a.includes(b);