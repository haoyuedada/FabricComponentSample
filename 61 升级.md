# React Native 从 0.61 升级至 0.72 适配记录

## 文档概述
本文档系统梳理了将项目从 **React Native 0.61** 升级至 **0.72** 版本过程中的核心差异、遇到的问题及相应的适配方案，旨在为后续的迁移与维护提供清晰的参考。

> **最后核对版本**：React Native 0.72.5

---

## 一、核心 API 与全局对象变更

### 1. NativeModules 对象访问方式变更
**问题场景**:
```javascript
import { NativeModules } from 'react-native';
// 尝试为NativeModules添加属性
NativeModules.xxx = yyy;
```
**版本差异**:
- 0.61：NativeModules 是一个普通的 JavaScript 对象，包含所有已注册的原生模块，可直接修改其属性。
- 0.72：NativeModules 是一个 Proxy 对象，无法直接进行属性增删改操作，也不直接暴露所有已注册模块

**适配方案**:  
在框架侧对 NativeModules 进行浅拷贝，确保其可读写，并将已注册的原生模块显式赋值给该拷贝对象。

### 2. Platform.select 方法
**问题场景**:
```javascript
const buttonColor = Platform.select({
  ios: 'blue',
  android: 'green',
});
```
**适配方案**:  
业务侧适配或者框架侧修改 Platform.select 的逻辑，当未匹配到明确的平台键时，默认回退至 ios｜android 对应的值，以确保兼容性。

### 3. UIManager 命令派发变更
```javascript
UIManager.dispatchViewManagerCommand(
  findNodeHandle(boxRef.current),
  // 新的 Fabric 架构下，UIManager 可能无法直接获取到 FabricComponents
  UIManager.SampleFabricComponents.commands.scrollTo,
  [!mutil, '23', [obj]],
);
```
**版本差异**:
- 0.61：UIManager 可以直接访问到原生模块。
- 0.72：在新的 Fabric 架构下，UIManager 可能无法直接获取到 FabricComponents。
适配方案：  
业务侧适配  
```javascript
UIManager.dispatchViewManagerCommand(
  findNodeHandle(boxRef.current),
  "scrollTo"
  [!mutil, '23', [obj]],
);
```

## 二、组件、样式与状态管理
### 4. 组件样式属性写法兼容性
**问题场景**:

```javascript
<View 
    style={{width: 50, height: 50}} 
    marginTop={200}>
</View>
```
**版本差异**:
- 0.61：支持将部分样式属性（如 marginTop）直接作为 props 写在 style 外部。
- 0.72：部分样式属性不再支持此种写法。
### setState 同步行为变更
**问题场景**:
```javascript
this.state = {
    isClose: false
};
Promise.resolve().then(() => {
  this.setState({ isClose: true });
  // 紧接着读取状态
  this.isClose ? this.close() : this.open();
})
```
**版本差异**:
- React16：setState 在特定情境下表现出同步行为，可立即获取更新后的 state 值。
- React18+：setState 始终为异步行为，无法在同一同步代码块中立即获取更新后的值（尤其在启用 React 18 并发特性时）。
**适配方案**:  
原生侧创建 RNInstance 时禁用 React 18 的并发渲染特性
## 三、构建、打包
**问题场景**:
自定义打包逻辑，用到了 babel，transform 相关的插件
```javascript
// 0.61 引用方式
require("node_modules/@react-native-community/cli/node_modules/metro-babel-transformer/src");
// 0.72 引用方式
require('metro-react-native-babel-transformer/src/index.js');

// 0.61
const babelRegisterOnly = require('metro/src/babelRegisterOnly');
// 0.72
const babelRegisterOnly = require('metro-babel-register');
```
