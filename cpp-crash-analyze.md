---

name: cxx-vulnerability-review
description: 识别 C++ 实现中的 crash、内存安全、未定义行为、资源泄漏、竞态和输入校验问题。适用于源码、patch、diff 的静态审查；输出问题、证据、影响和可执行修复建议。
---------------------------------------------------------------------------------------------------

# C++ 漏洞识别 Skill

## 目标

用于审查 C++ 代码中的安全性、稳定性和正确性问题，重点识别可能导致崩溃、内存破坏、未定义行为、资源泄漏、竞态条件，或可由外部输入触发的拒绝服务问题。

## 重点检查项

* 空指针解引用
* 越界访问
* Use-after-free
* Double free
* 缓冲区溢出 / 下溢
* 使用未初始化内存
* 整数溢出 / 截断
* 未定义行为（UB）
* 资源泄漏
* 数据竞争 / 死锁
* 不安全的输入处理
* 危险类型转换与裸内存 API
* 异常路径未清理资源

## 分析流程

0. 先询问用户代码仓库来源：是本地仓库、线上仓库，还是仅提供了单个文件/片段。如果是仓库，优先获取仓库地址、分支、提交号或关键目录；如果是本地路径，则确认需要分析的目录范围。

1. 识别入口点和信任边界。

2. 追踪对象、缓冲区、锁、文件、线程的所有权和生命周期。

3. 检查所有控制流分支，尤其是早退和异常路径。

4. 审查类型转换、指针运算、下标访问和长度计算。

5. 检查并发假设和共享状态。

6. 判断可利用性：问题是否可被不可信输入触发，影响有多大。

7. 给出具体修复方案，优先使用 RAII、边界检查、更安全的容器和显式同步。

## 输出格式

输出结果应整理为一个 Markdown 文档（md），便于直接保存、分享或纳入审查记录。

每个问题都按以下格式输出：

* **标题**
* **位置**：文件、行号、函数名（如果可定位）
* **类型**：crash、UAF、OOB、leak、race、UB、注入、DoS 等
* **证据**：关键代码片段或明确的行为链
* **原因**：为什么会出问题
* **影响**：可能造成的后果
* **触发条件**：什么输入或状态下会触发
* **修复建议**：具体到代码层面的修改
* **优先级**：P0 / P1 / P2
* **可复现 Demo**：给出最小可复现示例、触发输入、调用方式或伪代码；如条件不足，说明如何构造复现环境

同时给出一个简短总览，说明整体风险等级和最严重的问题。

## 判定原则

* 以证据为准，不凭感觉下结论。
* 对于 Harmony 官方 C++ 接口，优先以官方文档(https://developer.huawei.com/consumer/cn/doc/harmonyos-references/arkui-c)为准；当代码中调用到 Harmony ArkUI C++ 相关 API 时，必须先查证官方文档，再做语义判断。
* 不要根据通用 C++ 习惯臆测 Harmony API 的参数含义、返回值、生命周期、线程模型或异常行为；如果文档未明确说明，必须标注“未确认”或“需查文档确认”。
* 若分析中涉及 Harmony 平台接口，优先参考华为开发者联盟的官方 HarmonyOS 文档中心及相关 ArkUI C++ 参考文档。
* 不确定时明确写“可能”“疑似”“需要进一步确认”。
* 区分“确认存在的问题”和“风格或可维护性问题”。
* 对外部可达路径，默认更高风险。
* 优先给出具体代码修改建议，而不是泛泛而谈。

## 可复用提示词

> 请审查以下 C++ 代码是否存在 crash、内存安全、未定义行为、资源泄漏、竞态条件和输入校验问题。请逐项列出问题位置、触发条件、风险等级、证据和修复建议；如果证据不足，请明确说明不确定性，不要猜测。
