【react-native-maps】
1.支持地图类型：satellite、hybrid、mutedStandard、satelliteFlyover、hybridFlyover
2. Heatmap组件属性：points、gradient、radius、opacity
3. MapView组件属性：showsIndoors、showsIndoorLevelPicker、googleMapId、googleRenderer、kmlSrc、cacheEnabled、moveOnMarkerPress、toolbarEnabled、userLocationPriority、userLocationUpdateInterval、userLocationFastestInterval、cameraZoomRange、legalLabelInsets、tintColor、userLocationAnnotationTitle、userLocationCalloutEnabled、paddingAdjustmentBehavior、scrollDuringRotateOrZoomEnabled、zoomTapEnabled
4. MapView组件方法：setRegion()、enableLatestRenderer()、setIndoorActiveLevelIndex()、fitToElements()、fitToSuppliedMarkers()、getMarkersFrames()
5. MapView组件事件回调：onUserLocationChange、onCalloutPress、onMarkerSelect、onMarkerDeselect、onDoublePress、onPanDrag、onKmlReady、onMapLoaded、onIndoorBuildingFocused、onIndoorLevelActivated
6. Marker组件属性：tracksViewChanges、tracksInfoWindowChanges、stopPropagation
7. MapMarker类方法：redrawCallout()、animateMarkerToCoordinate()
react-native-maps的Polyline组件属性：geodesic、miterLimit、lineDashPhase、lineDashPattern
8. Polygon组件属性：geodesic
9. MapCircle类属性：lineCap、lineJoin、miterLimit
10. MapUrlTile类属性：tileCachePath、tileCacheMaxAge、offlineMode

【pull-to-refresh】
1、PullToRefreshHeader增加setNativeRefreshing 命令方式实现
1、PullToRefreshFooter增加setNativeRefreshing 命令方式实现


